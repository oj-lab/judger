[package]
name = "judge-core"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"
libseccomp = "0.3"
nix = { version = "0.26", features = ["fs", "event"] }
log = "0.4"
anyhow = "1.0"

[dev-dependencies]
env_logger = "0.10"
